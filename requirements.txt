# Smart Student Performance Analyzer — Python Dependencies
# Install with: pip install -r requirements.txt
# Python >= 3.10 required (uses X | Y union type hints)

# ── Core Data Science ───────────────────────────────────────────
pandas==2.2.2               # DataFrame operations, CSV ingestion, feature engineering
numpy==1.26.4               # Numerical operations, array manipulation

# ── Machine Learning ────────────────────────────────────────────
scikit-learn==1.4.2         # LogisticRegression, StandardScaler, Pipeline, metrics

# ── Visualization ───────────────────────────────────────────────
matplotlib==3.8.4           # Core plotting library (histograms, bar charts, confusion matrix)
seaborn==0.13.2             # Statistical plots (boxplot, heatmap) built on matplotlib

# ── Web Interface ───────────────────────────────────────────────
streamlit==1.35.0           # Interactive web UI — zero-config deployment

# ── Optional (for extended functionality) ───────────────────────
# openpyxl==3.1.2           # Uncomment if Excel (.xlsx) export is needed
# plotly==5.22.0            # Uncomment for interactive charts (replaces matplotlib)
# sqlalchemy==2.0.30        # Uncomment for database backend (scalability path)
